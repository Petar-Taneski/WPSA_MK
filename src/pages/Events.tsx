import EventsContent from "@/components/eventss/EventsContent";
const Events = () => {
  return (
    <div className="page events-page">
      <EventsContent />
    </div>
  );
};

export default Events;
