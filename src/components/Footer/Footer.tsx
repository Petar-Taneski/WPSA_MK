import React from "react";
import ContactInfo from "../ContactInfo";

const Footer: React.FC = () => {
  return (
    <footer className="mx-auto bg-white mt-auto">
      <ContactInfo />
    </footer>
  );
};

export default Footer;
